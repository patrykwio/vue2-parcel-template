{
    "name": "{{name}}",
    "version": "0.0.1",
    "description": "{{description}}",
    "main": "index.js",
    "private": true,
    "scripts": {
        "build": "parcel build index.html --out-dir build",
        "build:start": "parcel index.html --out-dir build --port 8080",
        "build:nominify": "cross-env NODE_ENV=production parcel build index.html --no-minify",
        "dev": "cross-env NODE_ENV=development parcel index.html",
        "start": "npm run dev",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": ["{{keywords}}"],
    "author": "{{author}}",
    "license": "MIT",
    "dependencies": {
        "vue": "^2.5.16",
        "axios": "0.18.0",
        "vue-axios": "2.1.4",
		"vue-router": "^3.0.1"
    },
    "devDependencies": {
        "cross-env": "^5.2.0",
        "parcel-bundler": "^1.9.7",
        "parcel-plugin-vue": "^1.5.0"
    },
    "browserslist": [
        "> 1%",
        "last 2 versions",
        "not ie <= 8"
    ],
    "engines": {
        "node": ">= 6.0.0",
        "npm": ">= 3.0.0",
        "yarn": "^0.13.0"
    }
}